const Specialization = {
    FRONTEND: 'FRONTEND',
    BACKEND: 'BACKEND',
    DEVOPS: 'DEVOPS',
    UX_UI: 'UX/UI'
};

const SpecializationsEnum = Object.values(Specialization);
module.exports = SpecializationsEnum;